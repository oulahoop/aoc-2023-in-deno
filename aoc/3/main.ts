import { getInput } from "../../functions.ts";

export function solution() {
    const inputPart1 = getInput("3", "_part1");
    const inputPart2 = getInput("3", "_part2");

    console.log(solutionPart1(inputPart1));
    console.log(solutionPart2(inputPart2));
}

function solutionPart1(input: string[]): number {
    return 0;
}

function solutionPart2(input: string[]): number {
    return 0;
}